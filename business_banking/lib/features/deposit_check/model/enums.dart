enum ServiceResponseStatus { succeed, failed, unknown }
enum UserInputStatus { valid, invalid, unknown }
enum CheckImageType { front, back }
enum InputStatusType {
  depositAmount,
  userEmail,
  checkFrontImg,
  checkBackImg,
  unknown
}
