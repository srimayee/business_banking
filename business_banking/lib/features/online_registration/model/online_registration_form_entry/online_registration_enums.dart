enum ServiceResponseStatus { succeed, failed, unknown }
enum UserFormInputStatus { valid, invalid, unknown }
enum InputStatusType {
  cardNumber,
  cardHolderName,
  ssnLastFourDigits,
  email,
  userPassword,
  unknown
}
