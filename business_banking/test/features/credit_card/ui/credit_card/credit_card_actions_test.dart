// @dart=2.9

import 'package:business_banking/features/credit_card/bloc/credit_card_bloc.dart';
import 'package:flutter/material.dart';
import 'package:mockito/mockito.dart';

class CreditCardBlocMock extends Mock implements CreditCardBloc {}
class MockBuildContext extends Mock implements BuildContext {}

void main() {

  /*
  CreditCardBlocMock bloc;
  MockBuildContext context;
  Widget testWidget;
   */

  /*
  setUp(() {
    bloc = CreditCardBlocMock();
    context = MockBuildContext();
    testWidget = CFRouterScope(
      routeGenerator: BusinessBankingRouter.generate,
      initialRoute: BusinessBankingRouter.initialRoute,
      builder: (context) => MaterialApp.router(
        routeInformationParser: CFRouteInformationParser(),
        routerDelegate: CFRouterDelegate(context),
        debugShowCheckedModeBanner: false,
      ),);
  });

  tearDown(() {
    bloc = null;
    context = null;
  });
     */

  /*
  testWidgets('CreditCardActions navigateToCreditCardDetails', (WidgetTester tester) async {
    BuildContext _context;
    CreditCardActions actions = CreditCardActions(bloc);
    actions.navigateToCreditCardDetails(_context);
    verify(CFRouterScope.of(context).push(any)).called(1);
  });
   */








}
